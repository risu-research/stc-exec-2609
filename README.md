# stc-exec-2609

Public deterministic execution runner for a frozen tool-surface measurement study.

Scientific-authority rules are frozen separately. This repository contains only reproducible source fetching, extraction, metric computation, certificate generation, tests, and public CI.

Primary G4 analysis uses no LLMs and no stochastic components.
